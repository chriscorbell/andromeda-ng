services:
  andromeda-chat:
    container_name: andromeda-chat
    restart: unless-stopped
    build: .
    ports:
      - "3834:3001"
    env_file:
      - .env
    environment:
      PORT: 3001
      DB_PATH: /data/chat.db
    volumes:
      - chat_data:/data

volumes:
  chat_data:
